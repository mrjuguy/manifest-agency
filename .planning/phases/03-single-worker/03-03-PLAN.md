---
phase: 03-single-worker
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - .claude/skills/worker/execute.md
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Worker skill defines state machine for issue execution"
    - "Worker posts plan comment and waits for approval"
    - "Worker creates worktree after approval"
    - "Worker creates PR with metadata on completion"
    - "Worker handles failures with clear error reporting"
  artifacts:
    - path: ".claude/skills/worker/execute.md"
      provides: "Main worker execution skill"
      min_lines: 200
      contains: ["worker:planning", "worker:executing", "acquire_lock", "log_event"]
  key_links:
    - from: ".claude/skills/worker/execute.md"
      to: ".claude/skills/worker/lib/logger.sh"
      via: "source directive"
      pattern: "source.*lib/logger\\.sh"
    - from: ".claude/skills/worker/execute.md"
      to: ".claude/skills/worker/lib/lock.sh"
      via: "source directive"
      pattern: "source.*lib/lock\\.sh"
    - from: ".claude/skills/worker/execute.md"
      to: ".claude/skills/worker/lib/parser.sh"
      via: "source directive"
      pattern: "source.*lib/parser\\.sh"
    - from: ".claude/skills/worker/execute.md"
      to: "scripts/worktree-create.sh"
      via: "script call"
      pattern: "scripts/worktree-create\\.sh"
    - from: ".claude/skills/worker/execute.md"
      to: "scripts/github/pr-create.sh"
      via: "script call"
      pattern: "scripts/github/pr-create\\.sh"
---

<objective>
Create the main worker execution skill that orchestrates the full worker lifecycle.

Purpose: This is the core skill that Claude invokes to execute work from GitHub issues. It implements the state machine (ready -> planning -> awaiting-approval -> executing -> complete), coordinates worktree creation, and produces structured results via GitHub comments and PRs.

Output: `.claude/skills/worker/execute.md` - a Claude skill file that can be invoked with `claude --skill worker:execute --issue 42`.
</objective>

<execution_context>
@C:\Users\tyler\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tyler\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-single-worker/03-CONTEXT.md
@.planning/phases/03-single-worker/03-RESEARCH.md
@.planning/phases/03-single-worker/03-01-SUMMARY.md
@.planning/phases/03-single-worker/03-02-SUMMARY.md

# Existing infrastructure to integrate with
@scripts/worktree-create.sh
@scripts/worktree-remove.sh
@scripts/github/issue-comment.sh
@scripts/github/issue-update-status.sh
@scripts/github/pr-create.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create worker execution skill</name>
  <files>
    .claude/skills/worker/execute.md
  </files>
  <action>
Create `.claude/skills/worker/execute.md` - the main worker skill that Claude invokes.

The skill MUST implement:
1. **Argument parsing**: --issue (required), --force (optional), --status (optional)
2. **State machine**: worker:ready -> worker:planning -> worker:awaiting-approval -> worker:executing -> worker:complete (or worker:failed)
3. **Approval workflow**: Post plan comment, exit cleanly, resume on re-run after approval
4. **Worktree lifecycle**: Create after approval, single commit at end
5. **Error handling**: Log errors, post failure comment, release lock

Structure the skill as a markdown file with embedded bash implementation:

```markdown
# Worker Execute Skill

Execute work from a GitHub issue in an isolated worktree.

## Usage

```bash
claude --skill worker:execute --issue 42
claude --skill worker:execute --issue 42 --force    # Override stale lock
claude --skill worker:execute --issue 42 --status   # Show execution status
```

## Arguments

| Argument | Required | Description |
|----------|----------|-------------|
| --issue N | Yes | GitHub issue number to execute |
| --force | No | Override stale lock (use after crash detection) |
| --status | No | Show status from log file instead of executing |

## Workflow

1. **Lock acquisition** - Prevent concurrent execution
2. **Issue parsing** - Extract Summary, Acceptance Criteria from issue body
3. **Plan generation** - Analyze work and create execution plan
4. **Approval gate** - Post plan as comment, wait for reaction
5. **Worktree creation** - Isolate work from main branch
6. **Execution** - Perform the work (Claude's implementation)
7. **Commit & PR** - Single commit, create PR with metadata
8. **Cleanup** - Release lock, update issue status

## Implementation

<!-- SKILL_IMPLEMENTATION -->
```bash
#!/usr/bin/env bash
#
# Worker Execute Skill Implementation
# Orchestrates GitHub issue -> work -> PR lifecycle
#

set -euo pipefail

# --- Configuration ---
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(git rev-parse --show-toplevel)"
WORKER_DIR="$REPO_ROOT/.worker"
LIB_DIR="$REPO_ROOT/.claude/skills/worker/lib"
SCRIPTS_DIR="$REPO_ROOT/scripts"

# --- Source libraries ---
source "$LIB_DIR/logger.sh"
source "$LIB_DIR/lock.sh"
source "$LIB_DIR/parser.sh"

# --- Parse arguments ---
ISSUE_NUMBER=""
FORCE_LOCK=false
STATUS_ONLY=false

while [[ $# -gt 0 ]]; do
  case $1 in
    --issue)
      ISSUE_NUMBER="$2"
      shift 2
      ;;
    --force)
      FORCE_LOCK=true
      shift
      ;;
    --status)
      STATUS_ONLY=true
      shift
      ;;
    --help)
      echo "Usage: claude --skill worker:execute --issue N [--force] [--status]"
      echo ""
      echo "Options:"
      echo "  --issue N   Issue number to execute (required)"
      echo "  --force     Override stale lock"
      echo "  --status    Show execution status from logs"
      exit 0
      ;;
    *)
      echo "[error] Unknown option: $1"
      exit 1
      ;;
  esac
done

# Validate required arguments
if [ -z "$ISSUE_NUMBER" ]; then
  echo "[error] --issue is required"
  echo "Usage: claude --skill worker:execute --issue N"
  exit 1
fi

# --- Status mode ---
if [ "$STATUS_ONLY" = true ]; then
  echo "[worker] Status for issue #${ISSUE_NUMBER}"
  echo ""

  # Check lock status
  check_lock_status "$ISSUE_NUMBER" 2>/dev/null || true
  echo ""

  # Find latest log file
  LOG_PATTERN="$WORKER_DIR/logs/issue-${ISSUE_NUMBER}-*.jsonl"
  LATEST_LOG=$(ls -t $LOG_PATTERN 2>/dev/null | head -1)

  if [ -n "$LATEST_LOG" ]; then
    echo "Latest log: $LATEST_LOG"
    echo ""
    echo "Recent events:"
    tail -5 "$LATEST_LOG" | jq -r '"  [\(.event_type)] \(.data | tostring)"'
  else
    echo "No log files found for issue #${ISSUE_NUMBER}"
  fi

  exit 0
fi

# --- Main execution ---
echo "[worker] Starting execution for issue #${ISSUE_NUMBER}"
echo ""

# Initialize logging
init_logging "$ISSUE_NUMBER"
log_event "worker_start" --arg issue "$ISSUE_NUMBER" --arg force "$FORCE_LOCK"

# Acquire lock
if ! acquire_lock "$ISSUE_NUMBER" "$FORCE_LOCK"; then
  log_event "lock_failed" --arg issue "$ISSUE_NUMBER"
  exit 1
fi

# Signal handler for graceful shutdown
WORKER_STATE="initializing"
handle_signal() {
  local signal="$1"
  log_event "signal_received" --arg signal "$signal" --arg state "$WORKER_STATE"

  if [ "$WORKER_STATE" = "executing" ]; then
    echo ""
    echo "[worker] Interrupted during execution - work may be incomplete"
    gh issue edit "$ISSUE_NUMBER" --add-label "worker:partial" 2>/dev/null || true
  fi

  log_session_end "interrupted" "$?"
  exit 130
}
trap 'handle_signal SIGTERM' SIGTERM
trap 'handle_signal SIGINT' SIGINT

# --- Check current issue state ---
WORKER_STATE="checking_state"
log_event "state_check" --arg issue "$ISSUE_NUMBER"

ISSUE_LABELS=$(gh issue view "$ISSUE_NUMBER" --json labels -q '.labels[].name' 2>/dev/null || echo "")

# If awaiting approval, check for approval
if echo "$ISSUE_LABELS" | grep -q "worker:awaiting-approval"; then
  echo "[worker] Issue in awaiting-approval state - checking for approval..."

  # Check for thumbs up reaction on plan comment
  PLAN_COMMENT_ID=$(gh api "/repos/{owner}/{repo}/issues/${ISSUE_NUMBER}/comments" \
    --jq '.[] | select(.body | contains("Execution Plan")) | .id' 2>/dev/null | tail -1)

  if [ -n "$PLAN_COMMENT_ID" ]; then
    APPROVAL_COUNT=$(gh api "/repos/{owner}/{repo}/issues/comments/${PLAN_COMMENT_ID}/reactions" \
      --jq '[.[] | select(.content == "+1")] | length' 2>/dev/null || echo "0")

    if [ "$APPROVAL_COUNT" -gt 0 ]; then
      echo "[worker] Approval received - proceeding with execution"
      log_event "approval_received" --arg comment_id "$PLAN_COMMENT_ID"

      # Remove awaiting-approval, add executing
      gh issue edit "$ISSUE_NUMBER" \
        --remove-label "worker:awaiting-approval" \
        --add-label "worker:executing" 2>/dev/null || true
    else
      echo "[worker] Plan not yet approved"
      echo ""
      echo "To approve: Add a thumbs up reaction to the Execution Plan comment"
      echo "Then re-run: claude --skill worker:execute --issue $ISSUE_NUMBER"
      log_session_end "awaiting_approval" "0"
      exit 0
    fi
  else
    echo "[error] Could not find plan comment - issue may be in inconsistent state"
    exit 1
  fi
fi

# --- Parse issue ---
WORKER_STATE="parsing"
echo "[worker] Parsing issue #${ISSUE_NUMBER}..."

if ! parse_issue_body "$ISSUE_NUMBER"; then
  log_event "parse_failed" --arg issue "$ISSUE_NUMBER"
  gh issue comment "$ISSUE_NUMBER" --body "## Worker Error

Failed to parse issue body. Please ensure the issue has:
- \`## Summary\` section
- \`## Acceptance Criteria\` section

See issue template for expected format."
  log_session_end "parse_error" "1"
  exit 1
fi

log_event "issue_parsed" \
  --arg title "$ISSUE_TITLE" \
  --arg summary "$ISSUE_SUMMARY" \
  --arg acceptance "$ISSUE_ACCEPTANCE"

echo "[worker] Issue: $ISSUE_TITLE"
echo "[worker] Labels: $ISSUE_LABELS"

# Validate required sections
if ! validate_issue_for_worker; then
  log_session_end "validation_error" "1"
  exit 1
fi

# --- Generate execution plan ---
WORKER_STATE="planning"
echo ""
echo "[worker] Generating execution plan..."

# Set planning label
gh issue edit "$ISSUE_NUMBER" --add-label "worker:planning" 2>/dev/null || true

# Get current commit SHA for traceability
BASE_SHA=$(git rev-parse HEAD)

# Plan comment body (Claude will analyze issue and generate actual plan)
PLAN_BODY="## Execution Plan

**Issue:** #${ISSUE_NUMBER} - ${ISSUE_TITLE}

**Summary:**
${ISSUE_SUMMARY}

**Acceptance Criteria:**
${ISSUE_ACCEPTANCE}

**Based on:** \`main @ ${BASE_SHA:0:7}\`

---

**Proposed approach:**

_Claude will analyze the issue and propose specific implementation steps here._

---

**Next:** React with :+1: to approve execution

<!-- agent:metadata
status: awaiting_approval
agent: claude-worker
plan_generated_at: $(date -u +"%Y-%m-%dT%H:%M:%SZ")
base_sha: $BASE_SHA
-->"

# Post plan comment
echo "[worker] Posting execution plan..."
gh issue comment "$ISSUE_NUMBER" --body "$PLAN_BODY"

log_event "plan_posted" --arg base_sha "$BASE_SHA"

# Update labels
gh issue edit "$ISSUE_NUMBER" \
  --remove-label "worker:planning" \
  --add-label "worker:awaiting-approval" 2>/dev/null || true

echo ""
echo "[worker] Plan posted to issue #${ISSUE_NUMBER}"
echo ""
echo "Next steps:"
echo "  1. Review the execution plan in the issue comment"
echo "  2. React with thumbs up to approve"
echo "  3. Re-run: claude --skill worker:execute --issue $ISSUE_NUMBER"

log_session_end "awaiting_approval" "0"
exit 0

# --- Execution phase (reached after approval) ---
# Note: This code is reached when re-running after approval

WORKER_STATE="executing"
START_TIME=$(date +%s)

echo ""
echo "[worker] Starting execution..."
log_event "execution_start" --arg timestamp "$START_TIME"

# Create worktree
BRANCH_NAME="issue/${ISSUE_NUMBER}"
WORKTREE_PATH="../$(basename "$REPO_ROOT")-issue-${ISSUE_NUMBER}"

echo "[worker] Creating worktree: $WORKTREE_PATH"
"$SCRIPTS_DIR/worktree-create.sh" "$BRANCH_NAME" "main"

log_event "worktree_created" \
  --arg path "$WORKTREE_PATH" \
  --arg branch "$BRANCH_NAME"

# Change to worktree
cd "$WORKTREE_PATH"
echo "[worker] Working in: $(pwd)"

# ===== EXECUTION PLACEHOLDER =====
# This is where Claude performs the actual work based on the issue
# The skill provides the orchestration framework; Claude provides the implementation
#
# Example work patterns:
# - Read acceptance criteria
# - Modify files as needed
# - Run tests
# - Stage changes with: git add <files>
#
# Claude should:
# 1. Analyze ISSUE_SUMMARY and ISSUE_ACCEPTANCE
# 2. Implement the required changes
# 3. Verify acceptance criteria are met
# 4. Stage all changes
# ==================================

echo ""
echo "[worker] Execution complete - creating commit..."

# Single commit with all changes
git add -A

COMMIT_MSG="$(echo "$ISSUE_TITLE" | sed 's/\[.*\]//' | xargs)

Resolves #${ISSUE_NUMBER}

Changes:
- Implementation per acceptance criteria

Co-Authored-By: Claude Worker <claude@anthropic.com>"

git commit -m "$COMMIT_MSG" || {
  echo "[warning] No changes to commit"
  log_event "no_changes" --arg issue "$ISSUE_NUMBER"
}

# Push branch
echo "[worker] Pushing branch..."
git push -u origin "$BRANCH_NAME"

log_event "branch_pushed" --arg branch "$BRANCH_NAME"

# Create PR
echo "[worker] Creating PR..."
cd "$REPO_ROOT"

PR_OUTPUT=$("$SCRIPTS_DIR/github/pr-create.sh" \
  --issue "$ISSUE_NUMBER" \
  --agent "claude-worker" \
  --worktree "$WORKTREE_PATH" \
  --start-time "$START_TIME")

PR_URL=$(echo "$PR_OUTPUT" | grep "URL:" | awk '{print $2}')
PR_NUMBER=$(echo "$PR_URL" | grep -oE '[0-9]+$')

log_event "pr_created" \
  --arg pr_number "$PR_NUMBER" \
  --arg pr_url "$PR_URL"

# Calculate duration
END_TIME=$(date +%s)
DURATION_SECONDS=$((END_TIME - START_TIME))
DURATION_MINUTES=$((DURATION_SECONDS / 60))
DURATION="${DURATION_MINUTES}min"

# Post completion comment
COMPLETE_BODY="## Work Complete

Created PR #${PR_NUMBER}

<details>
<summary>Execution Summary</summary>

**Duration:** $DURATION
**Branch:** \`$BRANCH_NAME\`
**Commits:** 1

**Acceptance Criteria Status:**
$(echo "$ISSUE_ACCEPTANCE" | sed 's/- \[ \]/- [x]/g')

</details>

<!-- agent:metadata
status: complete
agent: claude-worker
completed_at: $(date -u +"%Y-%m-%dT%H:%M:%SZ")
duration: $DURATION
pr_number: $PR_NUMBER
-->"

gh issue comment "$ISSUE_NUMBER" --body "$COMPLETE_BODY"

# Update labels
gh issue edit "$ISSUE_NUMBER" \
  --remove-label "worker:executing" \
  --add-label "worker:complete" 2>/dev/null || true

# Update project status
"$SCRIPTS_DIR/github/issue-update-status.sh" "$ISSUE_NUMBER" "In Review" 2>/dev/null || true

echo ""
echo "[worker] Execution complete"
echo "  PR: $PR_URL"
echo "  Duration: $DURATION"

log_event "execution_complete" \
  --arg pr_number "$PR_NUMBER" \
  --arg duration "$DURATION"

log_session_end "complete" "0"
```
<!-- END_SKILL_IMPLEMENTATION -->

## State Machine

```
    +---------+
    | ready   | (issue exists, no worker label)
    +----+----+
         |
         v acquire_lock + parse
    +----+----+
    | planning| (worker:planning label)
    +----+----+
         |
         v post plan comment
    +----+--------+
    | awaiting-  | (worker:awaiting-approval label)
    | approval   | (exit, wait for human approval)
    +----+--------+
         |
         v re-run after approval
    +----+----+
    |executing| (worker:executing label)
    +----+----+
         |
         v create PR
    +----+----+
    |complete | (worker:complete label)
    +---------+

Error states:
- worker:partial (interrupted during execution)
- worker:failed (error during execution)
```

## Labels

| Label | Meaning |
|-------|---------|
| worker:planning | Worker is analyzing issue and generating plan |
| worker:awaiting-approval | Plan posted, waiting for human approval |
| worker:executing | Worker is implementing the work |
| worker:complete | Work complete, PR created |
| worker:partial | Execution interrupted, partial work may exist |
| worker:failed | Execution failed, see error comment |
```

Important implementation notes:

1. **Library sourcing**: Source all three libraries from .claude/skills/worker/lib/
2. **State via labels**: Use GitHub labels as the state machine - this survives worker crashes
3. **Approval flow**: Post plan, exit cleanly, re-run detects awaiting-approval state
4. **Worktree naming**: Use issue/{N} branch and ../repo-issue-N/ path
5. **Single commit**: Stage all changes, one commit with "Resolves #N"
6. **Existing scripts**: Delegate to scripts/worktree-create.sh, scripts/github/pr-create.sh
7. **Structured logging**: Log every state transition and significant event
8. **Error comments**: Post GitHub comment with error details on failure
  </action>
  <verify>
```bash
# Verify skill file exists and has required content
echo "=== Skill File Check ==="
test -f .claude/skills/worker/execute.md && echo "OK: Skill file exists"

echo ""
echo "=== Required Components ==="
grep -q "source.*lib/logger.sh" .claude/skills/worker/execute.md && echo "OK: Sources logger"
grep -q "source.*lib/lock.sh" .claude/skills/worker/execute.md && echo "OK: Sources lock"
grep -q "source.*lib/parser.sh" .claude/skills/worker/execute.md && echo "OK: Sources parser"
grep -q "worker:planning" .claude/skills/worker/execute.md && echo "OK: Has planning state"
grep -q "worker:executing" .claude/skills/worker/execute.md && echo "OK: Has executing state"
grep -q "worker:awaiting-approval" .claude/skills/worker/execute.md && echo "OK: Has approval state"

echo ""
echo "=== Script References ==="
grep -q "worktree-create.sh" .claude/skills/worker/execute.md && echo "OK: References worktree-create"
grep -q "pr-create.sh" .claude/skills/worker/execute.md && echo "OK: References pr-create"

echo ""
echo "=== Line Count ==="
wc -l .claude/skills/worker/execute.md
```
  </verify>
  <done>
    - .claude/skills/worker/execute.md exists with full implementation
    - Sources all three library files (logger, lock, parser)
    - Implements state machine with GitHub labels
    - Handles approval workflow (exit and re-run pattern)
    - Creates worktree via scripts/worktree-create.sh
    - Creates PR via scripts/github/pr-create.sh
    - Logs all events via log_event
    - Posts structured GitHub comments at milestones
  </done>
</task>

<task type="auto">
  <name>Task 2: Create GitHub labels for worker states</name>
  <files>
    (GitHub API - no file created)
  </files>
  <action>
Create the worker state labels in GitHub using gh CLI.

Labels to create:
- `worker:planning` - Yellow (#FBCA04) - Worker is generating execution plan
- `worker:awaiting-approval` - Orange (#D93F0B) - Plan posted, waiting for approval
- `worker:executing` - Blue (#0052CC) - Worker is executing the work
- `worker:complete` - Green (#0E8A16) - Work complete, PR created
- `worker:partial` - Purple (#5319E7) - Execution interrupted, partial work
- `worker:failed` - Red (#B60205) - Execution failed

Run these commands:

```bash
# Create worker state labels
gh label create "worker:planning" --color "FBCA04" --description "Worker is generating execution plan" 2>/dev/null || echo "Label exists"
gh label create "worker:awaiting-approval" --color "D93F0B" --description "Plan posted, waiting for approval" 2>/dev/null || echo "Label exists"
gh label create "worker:executing" --color "0052CC" --description "Worker is executing the work" 2>/dev/null || echo "Label exists"
gh label create "worker:complete" --color "0E8A16" --description "Work complete, PR created" 2>/dev/null || echo "Label exists"
gh label create "worker:partial" --color "5319E7" --description "Execution interrupted, partial work" 2>/dev/null || echo "Label exists"
gh label create "worker:failed" --color "B60205" --description "Execution failed" 2>/dev/null || echo "Label exists"

echo ""
echo "Labels created. Verify with: gh label list | grep worker"
```

These labels form the state machine visible in GitHub UI.
  </action>
  <verify>
```bash
# List worker labels
gh label list | grep "worker:"

# Should show 6 labels:
# worker:planning, worker:awaiting-approval, worker:executing
# worker:complete, worker:partial, worker:failed
```
  </verify>
  <done>
    - 6 worker state labels exist in GitHub
    - Labels have descriptive colors (yellow/orange/blue/green/purple/red)
    - Labels have descriptions explaining their purpose
  </done>
</task>

</tasks>

<verification>
Overall plan verification:

```bash
echo "=== Worker Skill Structure ==="
find .claude/skills/worker -type f | sort

echo ""
echo "=== Skill Content Check ==="
head -50 .claude/skills/worker/execute.md

echo ""
echo "=== GitHub Labels ==="
gh label list | grep "worker:" | wc -l
echo "labels created"

echo ""
echo "=== Integration Test (dry run) ==="
# This would test the skill but requires a real issue
echo "To test: Create a test issue and run:"
echo "  claude --skill worker:execute --issue <number>"
```
</verification>

<success_criteria>
1. `.claude/skills/worker/execute.md` exists with complete implementation
2. Skill sources all three library files
3. State machine implemented via GitHub labels
4. Approval workflow exits cleanly and resumes on re-run
5. Worktree and PR creation delegate to existing scripts
6. All 6 worker state labels created in GitHub
7. Structured logging throughout the workflow
</success_criteria>

<output>
After completion, create `.planning/phases/03-single-worker/03-03-SUMMARY.md`
</output>
