---
phase: 02-github-coordination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .cache/.gitkeep
  - .cache/github/.gitkeep
  - templates/github/issue-body.md
  - templates/github/pr-body.md
  - templates/github/progress-comment.md
autonomous: false

must_haves:
  truths:
    - "jq is installed and accessible via command line"
    - "Cache directory exists for GitHub field ID caching"
    - "Templates exist for issue bodies, PR bodies, and progress comments"
  artifacts:
    - path: ".cache/github/.gitkeep"
      provides: "Cache directory for GraphQL field ID caching"
    - path: "templates/github/issue-body.md"
      provides: "Structured issue body template with all sections"
      contains: "Acceptance Criteria"
    - path: "templates/github/pr-body.md"
      provides: "Structured PR body template with agent metadata"
      contains: "Agent Metadata"
    - path: "templates/github/progress-comment.md"
      provides: "Progress comment template with HTML comment blocks"
      contains: "agent:progress"
  key_links: []
---

<objective>
Install prerequisites and create template files for GitHub coordination infrastructure.

Purpose: Establish the foundation that all subsequent GitHub scripts depend on - jq for JSON filtering and templates for consistent issue/PR formatting.

Output: jq installed, cache directory created, and three template files ready for use by scripts.
</objective>

<execution_context>
@C:\Users\tyler\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tyler\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-coordination/02-CONTEXT.md
@.planning/phases/02-github-coordination/02-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install jq JSON processor</name>
  <what-needed>jq is required for filtering GitHub API JSON responses. It is NOT currently installed on this system.</what-needed>
  <how-to-install>
    Open a NEW terminal (not this one) and run ONE of:

    **Option A - Chocolatey (if installed):**
    ```
    choco install jq
    ```

    **Option B - Scoop (if installed):**
    ```
    scoop install jq
    ```

    **Option C - Manual download:**
    1. Go to https://jqlang.github.io/jq/download/
    2. Download jq-win64.exe
    3. Rename to jq.exe
    4. Add to PATH
  </how-to-install>
  <verify>After installation, run: `jq --version` - should show jq-1.7 or higher</verify>
  <resume-signal>Type "jq installed" when ready, or "skip jq" if you want to proceed without it (scripts will fail without jq)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create cache directory structure</name>
  <files>.cache/.gitkeep, .cache/github/.gitkeep, .gitignore</files>
  <action>
    Create cache directory structure for GitHub API caching:

    1. Create `.cache/` directory with `.gitkeep` file
    2. Create `.cache/github/` subdirectory with `.gitkeep` file
    3. Update `.gitignore` to ignore cache contents but keep structure:
       - Add `.cache/*` to ignore cached files
       - Add `!.cache/.gitkeep` to keep the directory
       - Add `!.cache/github/.gitkeep` to keep the subdirectory

    The cache directory will store:
    - project-fields.json (GitHub Projects v2 field IDs, 1-hour TTL)
    - Rate limit status tracking
  </action>
  <verify>
    - `ls -la .cache/` shows directory exists with .gitkeep
    - `ls -la .cache/github/` shows subdirectory exists with .gitkeep
    - `git check-ignore .cache/test.json` returns 0 (file would be ignored)
    - `git check-ignore .cache/.gitkeep` returns 1 (file NOT ignored)
  </verify>
  <done>Cache directory structure exists and is correctly gitignored</done>
</task>

<task type="auto">
  <name>Task 3: Create GitHub templates</name>
  <files>templates/github/issue-body.md, templates/github/pr-body.md, templates/github/progress-comment.md</files>
  <action>
    Create three template files based on CONTEXT.md decisions:

    **templates/github/issue-body.md:**
    ```markdown
    ## Summary
    <!-- One-line description of the work item -->

    ## Context
    <!-- Background information, links to related issues/docs -->

    ## Acceptance Criteria
    - [ ] Criterion 1
    - [ ] Criterion 2

    ## Subtasks
    - [ ] Subtask 1
    - [ ] Subtask 2

    ## Dependencies
    <!-- Blocked by: #issue-number (if any) -->

    ## Parent
    <!-- Parent issue: #issue-number (if this is a subtask) -->

    ## Notes
    <!-- Implementation hints, relevant files, constraints -->
    ```

    **templates/github/pr-body.md:**
    ```markdown
    ## Summary
    <!-- Brief description of changes -->

    ## Changes
    <!-- Bullet list of modifications -->
    - Change 1
    - Change 2

    ## Testing
    - [ ] Unit tests pass
    - [ ] Integration tests pass
    - [ ] Manual testing complete

    Closes #ISSUE_NUMBER

    ---
    **Agent Metadata**
    | Field | Value |
    |-------|-------|
    | Agent | AGENT_NAME |
    | Issue | #ISSUE_NUMBER |
    | Worktree | WORKTREE_NAME |
    | Duration | DURATION |
    | Model | MODEL_NAME |
    | Trust Level | TRUST_LEVEL |
    | Commits | COMMIT_COUNT |
    ```

    **templates/github/progress-comment.md:**
    ```markdown
    HUMAN_MESSAGE

    <!-- agent:progress -->
    | Field | Value |
    |-------|-------|
    | Agent | AGENT_NAME |
    | Status | STATUS |
    | Progress | PROGRESS_PCT% |
    | Elapsed | ELAPSED_TIME |
    | Current Task | CURRENT_TASK |
    | Blockers | BLOCKERS |
    <!-- /agent:progress -->
    ```

    Use uppercase placeholders (ISSUE_NUMBER, AGENT_NAME, etc.) that scripts will replace.
  </action>
  <verify>
    - `ls templates/github/` shows 3 .md files
    - `grep "Acceptance Criteria" templates/github/issue-body.md` finds the section
    - `grep "Agent Metadata" templates/github/pr-body.md` finds the section
    - `grep "agent:progress" templates/github/progress-comment.md` finds the HTML comment
  </verify>
  <done>Three template files exist with correct structure and placeholders</done>
</task>

</tasks>

<verification>
1. `jq --version` returns jq-1.7 or higher
2. `.cache/github/` directory exists
3. All three templates exist in `templates/github/`
4. Templates contain expected sections (grep verification)
</verification>

<success_criteria>
- jq is installed and verified
- Cache directory structure is in place and correctly gitignored
- Three template files are ready for use by scripts
- No external service configuration required beyond jq installation
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-coordination/02-01-SUMMARY.md`
</output>
