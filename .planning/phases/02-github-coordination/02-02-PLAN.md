---
phase: 02-github-coordination
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - .cache/github/project-fields.json
  - scripts/github/project-bootstrap.sh
  - scripts/github/README.md
autonomous: false

must_haves:
  truths:
    - "GitHub Project exists with name 'Manifest Automations'"
    - "Project has all 6 custom fields: Status, Claimed By, Priority, Complexity, Agent Type, Phase"
    - "Project has 3 views: All Work (board), Ready Queue (table), By Phase (table)"
    - "Field IDs are cached locally for script use"
  artifacts:
    - path: "scripts/github/project-bootstrap.sh"
      provides: "Idempotent project setup script"
      contains: "gh api graphql"
    - path: ".cache/github/project-fields.json"
      provides: "Cached field IDs for API calls"
      contains: "projectV2"
  key_links:
    - from: "scripts/github/project-bootstrap.sh"
      to: "GitHub Projects v2 API"
      via: "gh api graphql mutations"
      pattern: "createProjectV2|updateProjectV2"
---

<objective>
Create and configure GitHub Project board with custom fields and views.

Purpose: Establish the visual coordination layer where all agent work is tracked - the kanban board that serves as the single source of truth for work state.

Output: A fully configured GitHub Project with 6 status columns, custom fields for agent metadata, and views for different workflows.
</objective>

<execution_context>
@C:\Users\tyler\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\tyler\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-github-coordination/02-CONTEXT.md
@.planning/phases/02-github-coordination/02-RESEARCH.md
@.planning/phases/02-github-coordination/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project bootstrap script</name>
  <files>scripts/github/project-bootstrap.sh, scripts/github/README.md</files>
  <action>
    Create an IDEMPOTENT project setup script that:

    1. **Check if project exists** - Query for project named "Manifest Automations" using gh api graphql
    2. **Create project if missing** - Use createProjectV2 mutation
    3. **Add/verify custom fields** (all as Single Select except Claimed By as Text):
       - Status: Backlog, Ready, Claimed, In Progress, In Review, Done
       - Claimed By: (Text field, not Single Select)
       - Priority: P0-critical, P1-high, P2-medium, P3-low
       - Complexity: XS, S, M, L, XL
       - Agent Type: agent:any, agent:code-reviewer, agent:backend-developer, agent:frontend-developer, agent:architect
       - Phase: phase:1, phase:2, phase:3, phase:4, phase:5, phase:6, phase:7
    4. **Cache field IDs** to `.cache/github/project-fields.json`
    5. **Be idempotent** - Running twice should produce same result (check before create)

    Script structure:
    ```bash
    #!/bin/bash
    # scripts/github/project-bootstrap.sh
    # Idempotent GitHub Project setup for Manifest Automations
    set -e

    # Configuration
    PROJECT_TITLE="Manifest Automations"
    OWNER="$(gh api user -q '.login')"  # Get authenticated user
    CACHE_FILE=".cache/github/project-fields.json"

    # Check for existing project
    # Create project if not exists
    # Add custom fields (check each first)
    # Cache field IDs
    ```

    Use `gh api graphql` for all mutations. Check existence before creating.

    Also create initial `scripts/github/README.md` documenting the bootstrap script usage.
  </action>
  <verify>
    - `bash -n scripts/github/project-bootstrap.sh` (syntax check passes)
    - Script contains idempotency checks (grep for "existing" or "already")
    - Script creates all 6 custom fields
  </verify>
  <done>Bootstrap script exists with idempotent project and field creation logic</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Execute bootstrap and verify project setup</name>
  <what-built>
    Project bootstrap script that creates:
    - GitHub Project "Manifest Automations"
    - 6 custom fields with correct options
    - Cached field IDs in .cache/github/project-fields.json
  </what-built>
  <how-to-verify>
    1. Run the bootstrap script:
       ```
       bash scripts/github/project-bootstrap.sh
       ```

    2. Verify project exists on GitHub:
       - Go to https://github.com/users/YOUR_USERNAME/projects
       - Find "Manifest Automations" project
       - Click into it

    3. Verify custom fields exist:
       - Click settings (gear icon) in project
       - Check "Fields" section shows: Status, Claimed By, Priority, Complexity, Agent Type, Phase

    4. Verify cache file created:
       ```
       cat .cache/github/project-fields.json | jq '.data'
       ```

    If bootstrap fails, check:
    - gh auth status (need repo scope)
    - Error messages for specific field that failed
  </how-to-verify>
  <resume-signal>Type "project ready" if project and fields verified, or describe any issues</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Create project views via API</name>
  <files>scripts/github/project-bootstrap.sh (append view creation)</files>
  <action>
    Extend the bootstrap script to create 3 views:

    1. **All Work (board view)** - Default kanban view with all statuses
       - Layout: Board
       - Group by: Status
       - Sort by: Priority (P0 first)

    2. **Ready Queue (table view)** - Filter to Status=Ready for agent claiming
       - Layout: Table
       - Filter: Status is "Ready"
       - Columns: Title, Priority, Complexity, Agent Type
       - Sort by: Priority

    3. **By Phase (table view)** - Grouped by phase for roadmap progress
       - Layout: Table
       - Group by: Phase
       - Columns: Title, Status, Priority, Assigned To

    Use `createProjectV2View` or equivalent GraphQL mutation.
    Make view creation idempotent (check if view exists first).

    Note: View creation via API may be limited. If not possible via API:
    - Document manual view creation steps
    - Add TODO comment in script
  </action>
  <verify>
    - Script includes view creation logic OR documents limitation
    - If views created via API, they appear in project UI
  </verify>
  <done>Views created or documented (API may have limitations)</done>
</task>

</tasks>

<verification>
1. GitHub Project "Manifest Automations" exists at github.com/users/{user}/projects
2. Project settings show 6 custom fields with correct types and options
3. `.cache/github/project-fields.json` contains valid field IDs
4. Views exist (or documented as manual step)
5. Running bootstrap again produces no errors (idempotent)
</verification>

<success_criteria>
- GitHub Project is the authoritative coordination layer
- Custom fields enable agent assignment, prioritization, and phase tracking
- Views provide different perspectives for work management
- Field IDs cached for use by subsequent scripts
</success_criteria>

<output>
After completion, create `.planning/phases/02-github-coordination/02-02-SUMMARY.md`
</output>
